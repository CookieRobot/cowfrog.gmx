///setOptionMatchSetting(height,length)
//this checks if an option object matches the current game settings and sets it as the default option

//it takes the designated height and length of the requested array position to set

// the manager's position
if (attribute == getSettings(name))
{
    print("Found a option-setting match")
    display.outFocus = true;
    display.selected = true;
    manager.height = argument0;
    manager.length = argument1;
}
